 {
  "clause_name": "ORDER BY",
  "methods": [
    {
      "name": "order_by",
      "docstring_summary": "Concatenate an order by statement",
      "args": {
        "sorting_properties": {
          "type": "Union[ReferenceProperties, str, List[Union[ReferenceProperties, str]]]",
          "description": "A reference-properties object for the desired properties to sort by, a string of cypher expression (that evaluates to a property) or a mixed list of them."
        },
        "ascending": {
          "type": "bool",
          "description": "Use ascending sorting (if false, uses descending).",
          "default": "True"
        }
      }
    }
  ],
  "successors": [
    "Limit",
    "Skip"
  ]
}